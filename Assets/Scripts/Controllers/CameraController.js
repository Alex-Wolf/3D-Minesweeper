#pragma strict

var moveSpeed:float = 20.0;
var turnSpeed:float = 50.0;


function Start() {
	
	//Screen.showCursor = false;
}

function Update() {
	
	/*var hr:float = Input.GetAxis("Mouse X") * Time.deltaTime * turnSpeed;
	var vr:float = Input.GetAxis("Mouse Y") * Time.deltaTime * turnSpeed;
	transform.Rotate(-vr, hr, 0);
	
	var zr:float = transform.eulerAngles.z;
	transform.Rotate(0, 0, -zr);
	
	var s:float = Input.GetAxis("Straif") * Time.deltaTime * moveSpeed;
	var f:float = Input.GetAxis("Forward") * Time.deltaTime * moveSpeed;
	var v:float = Input.GetAxis("Vertical") * Time.deltaTime * moveSpeed;
	transform.Translate(s, v, f);*/
}